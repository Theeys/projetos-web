import { Entity } from "./entity.js"

export class Explosion extends Entity {
    constructor(pos, size) {
        super(pos, size)
    }

    draw(ctx) {
        super.draw(ctx)
    }

    update(game) {
        super.update(game)
    }
}
